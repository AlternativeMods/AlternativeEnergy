package jkmau5.alternativeenergy;

/**
 * No description given
 *
 * @author jk-5
 */
public class AltEngSupportTest {

    /*@Test
    public void testInitiateNbtTag(){
        ItemStack stack = new ItemStack(0, 1, 0);
        Assert.assertNull(stack.getTagCompound());
        AltEngSupport.initiateNBTTag(stack);
        Assert.assertNotNull(stack.getTagCompound());
    }

    @Test
    public void testCanWrench(){
        ItemStack wrench = new ItemStack(0, 1, 0);
        NBTTagCompound tag = new NBTTagCompound();
        tag.setInteger("storedPower", 50);
        wrench.setTagCompound(tag);

        Assert.assertFalse(AltEngSupport.canWrench(wrench));
        tag.setInteger("storedPower", 250);
        Assert.assertTrue(AltEngSupport.canWrench(wrench));
        tag.setInteger("storedPower", 300);
        Assert.assertTrue(AltEngSupport.canWrench(wrench));
    }*/
}
